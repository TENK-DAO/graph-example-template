specVersion: 0.0.4
repository: https://github.com/r-strawser/tenk-subgraph-template.git
schema:
  file: schema.graphql
features:
  - fullTextSearch
dataSources:
  - kind: near
    name: nymphs
    network: near-mainnet
    source:
      account: nymphs.near
      startBlock: 61518436
    mapping:
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      entities:
        - Account
        - Token
        - Transfer
        - Contract
        - Mint
        - Burn
      receiptHandlers:
        - handler: handleReceipt
      file: nymphs/nymphs.wasm
